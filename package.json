{
  "scripts": {
    "server": " cd backend && npm start",
    "client": " cd frontend &&  npm start",
    "buildF": " cd frontend &&  npm build",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "concurrently \"npm run buildF\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  }
}
