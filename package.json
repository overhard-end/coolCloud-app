{
  "scripts": {
    "server": " cd backend && npm start",
    "client": " cd frontend &&  npm start",
    "buildF": " cd frontend &&  npm build",
    "buildS": " cd backend &&  npm build",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "concurrently \"npm run buildF\" \"npm run buildS\""
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "dependencies": {
    "react-dropzone": "^14.2.3",
    "socket.io-client": "^4.5.4"
  }
}
